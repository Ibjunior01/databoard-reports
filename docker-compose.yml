services:
  web:
    build: .
    container_name: databoard_reports_web
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=True
      - SECRET_KEY=dev-secret-key-change-me
    volumes:
      - .:/app